node_modules
dist
build
.DS_Store
*.log
.vscode
.idea

# Scratch planning notes (local only)
book-plan
plan-download

# brush-preview live-smoke screenshot (regenerated on each run)
tests/smoke/brush-preview-live.png

# layers reorder live-smoke screenshot (regenerated on each run)
tests/smoke/layers-reorder-live.png

# Built site artifacts - generated by build.sh / build:updates and published by
# the deploy-pages workflow. No longer committed (see .github/workflows/deploy-pages.yml).
docs/app/index.html
docs/updates.html
